services:
  - type: web
    name: recipe-backend
    runtime: python
    env: python-3.12.0
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
